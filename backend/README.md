# backend/

Owned by the backend (FastAPI) teammate. Not yet populated.

See ../docs/AI_INTEGRATION.md for exactly how to import and call the
AI module (`ai/services/feedback_analyzer.py`), plus the input/output
contract and a backend handoff checklist.
